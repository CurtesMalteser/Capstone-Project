<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/colorPrimary"
    android:padding="@dimen/eightDp">

    <TextView
        android:id="@+id/appwidget_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:contentDescription="@string/shows_the_next_event"
        android:lines="1"
        android:paddingEnd="@dimen/eightDp"
        android:paddingStart="@dimen/eightDp"
        android:textColor="@android:color/white"
        android:textSize="24sp"
        android:textStyle="bold|italic"
        tools:text="This is one event" />

    <TextView
        android:id="@+id/appwidget_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="@dimen/eightDp"
        android:contentDescription="@string/next_event_starts_description"
        android:lines="1"
        android:paddingEnd="@dimen/eightDp"
        android:paddingStart="@dimen/eightDp"
        android:textColor="#ffffff"
        android:textSize="@dimen/twelveFont"
        tools:text="This is one event" />
</LinearLayout>